plugins {
    id 'java-library'
}

group = 'com.cuevas'
version = '0.0.1-SNAPSHOT'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(24)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // Sin dependencias externas: common es una librería plana de DTOs/records.
}
